(ns art.config)


(def config {:canvas "art"
             :agent-count 100
             :size [13 13 13]
             :radius 3
             :cohesion 0
             :separation 0.04
             :alignment 0.05
             :max-swarm 0.08
             :speed 0.18
             :max-speed 0.4
             :trail-size 130})
