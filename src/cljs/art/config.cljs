(ns art.config)

(def config {:agent-count 30
             :size [10 10 10]
             :radius 30
             :cohesion 0.039
             :separation 0.04
             :alignment 0.06
             :max-vel 0.05
             :trail-size 50})
