(ns art.config)


(def config {:canvas "art"
             :agent-count 30
             :size [10 10 10]
             :radius 3
             :cohesion 0.039
             :separation 0.04
             :alignment 0.06
             :max-vel 0.2
             :trail-size (* 3 45)})
